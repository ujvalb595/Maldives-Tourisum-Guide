# TODO: Add Zoom-in Effect on Marker Icons

- [x] Create ZoomMarker.jsx component in src/Components/ that wraps Marker and adds zoom functionality using useMap
- [x] Update App.js to import ZoomMarker and replace Marker with ZoomMarker for category markers
- [x] Test the app to ensure zoom works and popup still opens
- [ ] Adjust zoom level if needed
